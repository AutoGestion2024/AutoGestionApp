<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoGestion"
        tools:targetApi="31">
        <activity
            android:name=".Home"
            android:exported="true"
            android:theme="@style/Theme.AutoGestion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- <activity android:name=".Home"/> -->
        <activity android:name=".Camera" />
        <activity android:name=".ClientProfile" />
        <activity android:name=".VehicleProfile" />
        <activity android:name=".form.ClientForm" />
        <activity android:name=".form.VehicleForm" />
        <activity android:name=".form.RepairForm" />
        <activity android:name=".form.ClientFormUpdate" />
        <activity android:name=".form.VehicleFormUpdate" />
        <activity android:name=".form.RepairFormUpdate" />
        <activity android:name=".form.VehicleFormAdd" />
        <activity android:name=".form.RepairFormAdd" />

    </application>


</manifest>
<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AutoGestion"
        tools:targetApi="31">

        <!-- Déclaration de l'Activity Home -->
        <activity
            android:name=".Home"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AutoGestion">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Déclaration de l'Activity HomeForm -->
        <activity android:name=".HomeForm"></activity>

        <!-- Déclaration de l'Activity ClientsDetails -->
        <activity android:name=".ClientDetails"></activity>

        <!-- Déclaration de l'Activity com.example.autogestion.ClientUpdatePage -->
        <activity android:name=".ClientUpdatePage"></activity>


    </application>

</manifest>